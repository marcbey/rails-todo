<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Railtalk" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Railtalk">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Tailwind build output lives in app/assets/builds %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-slate-950 text-slate-100">
    <div class="min-h-screen bg-[radial-gradient(circle_at_top,_rgba(59,130,246,0.15),_transparent_55%),radial-gradient(circle_at_bottom,_rgba(148,163,184,0.12),_transparent_55%)]">
      <header class="border-b border-slate-800/80 bg-slate-950/60 backdrop-blur">
        <div class="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
          <div class="flex items-center gap-3">
            <div class="flex h-9 w-9 items-center justify-center rounded-xl bg-slate-800 text-lg font-semibold">R</div>
            <div>
              <div class="text-lg font-semibold tracking-tight">Railtalk</div>
              <div class="text-xs text-slate-400">Hotwire team chat</div>
            </div>
          </div>
          <% if authenticated? %>
            <div class="flex items-center gap-4 text-sm text-slate-300">
              <span class="hidden sm:inline">Signed in as <span class="font-medium text-slate-100"><%= current_user.email_address %></span></span>
              <%= button_to "Sign out", session_path, method: :delete, class: "rounded-full border border-slate-700 px-3 py-1 text-xs font-semibold uppercase tracking-wide hover:border-slate-500 hover:text-white" %>
            </div>
          <% end %>
        </div>
      </header>

      <% if notice || alert %>
        <div class="mx-auto mt-4 max-w-6xl px-6">
          <div class="rounded-2xl border border-slate-800 bg-slate-900/70 px-4 py-3 text-sm">
            <%= notice || alert %>
          </div>
        </div>
      <% end %>

      <main class="mx-auto max-w-6xl px-6 py-8">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
